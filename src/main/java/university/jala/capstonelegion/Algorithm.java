package university.jala.capstonelegion;

public interface Algorithm {
    Object [][] orderByInsertion(int rows, int columns, Object[][] fields, Object types);

    // I will add the other algorithms soon.
}
